{
  "name": "express-prisma",
  "version": "1.0.0",
  "main": "src/main.ts",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "prisma validate && prisma format && prisma generate && prisma db push && tsx watch src/main.ts",
    "run": "tsx src/main.ts",
    "db:update": "prisma validate && prisma format && prisma generate && prisma db push"
  },
  "devDependencies": {
    "@types/connect-busboy": "^1.0.0",
    "@types/express": "^4.17.15",
    "@types/uuid": "^9.0.0",
    "prisma": "^4.8.1",
    "tsx": "^3.12.2",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@prisma/client": "4.8.1",
    "connect-busboy": "^1.0.0",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  }
}
