export interface ImageBlockInterface {
  id: string;
  title: string;
  mimetype: string;
  order: number;
}

export interface TextBlockInterface {
  id: string;
  title: string;
  body: string;
  order: number;
}